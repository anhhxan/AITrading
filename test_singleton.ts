const pollerBus = require('./src/worker/CommandPoller.ts');
const stratBus = require('./src/core/engine/strategies/StrategyEngine.ts');

async function test() {
    // wait I need to require tsx or ts-node to do this.
}
