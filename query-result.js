const { createClient } = require('@supabase/supabase-js'); require('dotenv').config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const { data: cmds } = await supabase.from('robot_commands').select('*').eq('robot_id', 'ab80dcb8-2a05-4a08-81f7-dbddb714b828').order('created_at', { ascending: false }).limit(1); console.log('COMMANDS:', cmds); const { data: pos } = await supabase.from('active_positions').select('*').eq('robot_id', 'ab80dcb8-2a05-4a08-81f7-dbddb714b828'); console.log('POSITIONS:', pos); } run();
